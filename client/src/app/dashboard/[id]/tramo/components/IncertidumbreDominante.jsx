// obtencion del id por parametro 
 /*  const pathname = usePathname(); // "/dashboard/1/senial"

  // 1. Dividimos por "/" -> ["", "dashboard", "1", "senial"]
  // 2. El "1" está en la posición 2 del array
  const segments = pathname.split('/');
  const id = segments[2];
 

  const project = getProjectById(id);
 


/*   import dataProjects from '@/lib/mock/proyectos ficticios/dataProjects.json';

export function getProjectById(id) {
  const index = Number(id) - 1; // convierte "1" → 0, "3" → 2
  const selected = dataProjects.projectsData[index];
  return selected || null;
} */